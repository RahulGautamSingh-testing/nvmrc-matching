{
  "regexManagers": [
    {
      "fileMatch": [".nvmrc"],
      "matchStrings": [
        "^(?<currentValue>.*)$"
      ],
      "depNameTemplate": "docker",
      "datasourceTemplate": "docker",
      "autoReplaceStringTemplate": "{{{newValue}}}"
    }
  ]
}
